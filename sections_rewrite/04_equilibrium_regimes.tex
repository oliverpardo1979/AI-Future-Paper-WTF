\section{Equilibrium regimes}
\label{sec:rewrite-regimes}

% Open with the mechanism that is common to all three regimes. Then change only
% sigma_XL. Do not use a terminal condition from one regime to solve another.

\subsection{Complementarity: \texorpdfstring{\(\sigma_{XL}<1\)}{sigma XL < 1}}
\label{subsec:rewrite-complements}

% 1. Labor bottleneck proposition.
% 2. Implications for output per person, wages, and labor's income share.
% 3. Local existence of an equilibrium trajectory near the complementary-input
%    terminal regime.

\subsection{Unit elasticity: \texorpdfstring{\(\sigma_{XL}=1\)}{sigma XL = 1}}
\label{subsec:rewrite-unit}

% 1. Analytical balanced-growth equilibrium for autonomous research.
% 2. Growth rates and income/resource shares, including g_w.
% 3. Conditions for developer optimality and both TVCs.
% 4. Local equilibrium trajectories away from the balanced-growth path.

\subsection{Gross substitution: \texorpdfstring{\(\sigma_{XL}>1\)}{sigma XL > 1}}
\label{subsec:rewrite-substitutes}

% 1. No finite-rate BGP when capability is unbounded.
% 2. Finite capability frontier and derivation of Bbar_c.
% 3. Labor-supported terminal regime below Bbar_c.
% 4. AI-dominated AK-type terminal regime above Bbar_c.
% 5. Local finite-cap equilibrium paths in both regimes.
% 6. Finite-window limit as Bbar tends to infinity and why this does not prove
%    an uncapped infinite-horizon equilibrium.

\subsection{What the three regimes establish}
\label{subsec:rewrite-regime-synthesis}

% Summarize only propositions already derived. Separate growth in levels and
% wages from labor's relative claim on output. State explicitly where the
% available result is local, conditional, or numerical.
